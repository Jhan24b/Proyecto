// reducers.js
const initialState = {
    // Define el estado inicial aquí
  };
  
  const rootReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define cómo el estado cambia en respuesta a las acciones
      default:
        return state;
    }
  };
  
  export default rootReducer;
  